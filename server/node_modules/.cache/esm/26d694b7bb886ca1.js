let mongoose,Joi;_bb8‍.x([["postValidate",()=>postValidate],["validateObjectId",()=>validateObjectId]]);_bb8‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_bb8‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);


const title = Joi.string()
  .min(5)
  .max(50)
  .required()
  .label('title')

const description = Joi.string()
  .min(5)
  .required()
  .label('description')

       const postValidate = Joi.object().keys({
  title,
  description
})

       const validateObjectId = (postId, commentId) => {
  if (!mongoose.Types.ObjectId.isValid(postId || commentId)) {
    throw new Error(`${postId || commentId} is not a valid user ID`)
  }
}
